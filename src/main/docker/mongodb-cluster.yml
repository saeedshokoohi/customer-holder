version: '2'
services:
  customerholder-mongodb:
    image: mongo:4.0.16
    ports:
      - '27017:27017'
    command: mongos --configdb customerholder-mongodb-config
  customerholder-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  customerholder-mongodb-config:
    image: mongo:4.0.16
    container_name: customerholder-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
